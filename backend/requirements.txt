# Pinned to a known-good set so `docker compose up --build` is reproducible
# and a newer upstream release can't silently break the container.
fastapi==0.138.0
starlette==1.3.1
uvicorn[standard]==0.49.0
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.14.0
pydantic==2.13.4
trafilatura==1.12.2
beautifulsoup4==4.15.0
lxml==5.4.0
